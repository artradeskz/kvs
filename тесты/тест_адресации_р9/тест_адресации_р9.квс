;./тесты/тест_адресации_р9.elf "A"
;echo $?   # должен быть 65

; ============================================================
; тест_адресации_r9.квс - тест косвенной адресации через R8
; ============================================================

.текст
.глобал _start

_start:
    ; Получаем argv[1]
    вытолкнуть рсикс                 ; argc
    вытолкнуть рсиай                 ; argv[0]
    вытолкнуть р9                    ; argv[1] в R9

    ; Загружаем первый байт из argv[1] через R9
    загрузить ал, [р9]

    ; Сохраняем в переменную (не обязательно, но для проверки)
    сохранить [байт], ал

    ; Выход с кодом = загруженный байт
    переместить_имм раикс, 60
    загрузить рдиай, [байт]
    вызов_системы

.данные
байт: .байт 0