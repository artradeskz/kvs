;./тесты/тест_адресации_р8.elf "A"
;echo $?   # должен быть 65

; ============================================================
; тест_адресации_r8.квс - тест косвенной адресации через R8
; ============================================================

.текст
.глобал _start

_start:
    ; Получаем argv[1]
    вытолкнуть рсикс                 ; argc
    вытолкнуть рсиай                 ; argv[0]
    вытолкнуть р8                    ; argv[1] в R8

    ; Загружаем первый байт из argv[1] через R8
    загрузить ал, [р8]

    ; Сохраняем в переменную (не обязательно, но для проверки)
    сохранить [байт], ал

    ; Выход с кодом = загруженный байт
    переместить_имм раикс, 60
    загрузить рдиай, [байт]
    вызов_системы

.данные
байт: .байт 0