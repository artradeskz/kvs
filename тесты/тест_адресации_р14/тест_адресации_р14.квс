;./тесты/тест_адресации_р14.elf "A"
;echo $?   # должен быть 65

; ============================================================
; тест_адресации_r14.квс - тест косвенной адресации через R14
; ============================================================

.текст
.глобал _start

_start:
    ; Получаем argv[1]
    вытолкнуть рсикс                 ; argc
    вытолкнуть рсиай                 ; argv[0]
    вытолкнуть р14                   ; argv[1] в R14

    ; Загружаем первый байт из argv[1] через R14
    загрузить ал, [р14]

    ; Сохраняем в переменную (не обязательно, но для проверки)
    сохранить [байт], ал

    ; Выход с кодом = загруженный байт
    переместить_имм раикс, 60
    загрузить рдиай, [байт]
    вызов_системы

.данные
байт: .байт 0